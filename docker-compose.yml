version: '3.8'
# Starts only Redis. Run the Python backend separately: uvicorn app.main:app --reload
# Run the Next.js frontend separately: npm run dev
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
