# Copy to .env.local inside the frontend/ directory.
# The backend must be running at this URL for the frontend to work.
NEXT_PUBLIC_API_URL=http://localhost:8000

# CopilotKit Node runtime orchestration (server-side only, not exposed to the browser).
ANTHROPIC_API_KEY=
