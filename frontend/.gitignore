# Build artifacts regenerated by `next build` — must NOT be tracked,
# otherwise every build dirties the working tree (and blocks the deploy poller).
/next-env.d.ts
/tsconfig.tsbuildinfo
/.next/
/out/
